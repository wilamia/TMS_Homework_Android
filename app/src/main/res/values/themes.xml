<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StudyingProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.StudyingProject" parent="Base.Theme.StudyingProject" />
    <style name="Widget.AppCompat.Button.NewButton">
        <item name="backgroundTint">@color/button_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:radius">100dp</item>
    </style>
</resources>